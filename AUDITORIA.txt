AUDITORÍA FINAL 2

Corrección importante:
- XcodeGen no usa una clave target-level `resources:` como se había configurado.
- Assets.xcassets y PrivacyInfo.xcprivacy se declaran ahora dentro de `sources`
  con `buildPhase: resources`, que es la forma documentada por XcodeGen.
- La auditoría comprueba el IPA real y detiene el build antes de publicar si falta:
  ejecutable, Assets.car, AppIcon, PrivacyInfo, Info.plist válido, extensión,
  perfiles o firmas.

No hace falta editar ningún archivo manualmente: este ZIP incluye project.yml,
codemagic.yaml, recursos, plists y el preflight actualizados.
